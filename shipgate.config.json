{
  "repo": "spert-landing-page",
  "versionConstant": {
    "file": "src/config.ts",
    "export": "APP_VERSION"
  },
  "_changelogNote": "There is no CHANGELOG.md in this repository \u2014 the changelog lives only in src/data/changelog.ts, rendered by src/app/changelog/page.tsx. So there is no markdown surface to check package.json against, and the `changelog` key is deliberately absent. Instead src/guards/changelog-renders.test.ts asserts that the newest entry matches APP_VERSION, which the version-surface check above ties back to package.json. Note this file's section key is `heading`, where every other app in the suite uses `title` \u2014 do not copy a sibling guard without adjusting.",
  "claudeMdVersionPatterns": [
    "^\\*\\*Version:\\*\\* (\\d+\\.\\d+\\.\\d+)"
  ],
  "_claudeMdNote": "Swept on 2026-07-30. The `**Version:** X.Y.Z` line is the only version declaration; the other X.Y.Z mentions are historical prose ('v2.5.6 cleared nine GHSAs') and are correct as written \u2014 never flag those. Skipped in CI, where this gitignored file is not checked out.",
  "commands": [
    {
      "name": "lint (root)",
      "run": "npm run lint"
    },
    {
      "name": "typecheck (root)",
      "run": "npm run typecheck"
    },
    {
      "name": "test (root guards)",
      "run": "npm test"
    },
    {
      "name": "test:rules (Firestore rules vs emulator)",
      "run": "npm run test:rules",
      "_note": "Runs the REAL firestore.rules against the Firestore emulator. This is the guard on LIST-1 (2026-08-19): every *_projects collection had shipped `allow list: if isAuth();`, letting any authenticated SPERT user list every project in the suite. It asserts BOTH directions \u2014 the unfiltered list is denied AND each of the seven apps' actual production queries still return their documents, checked down to document ids. A rule loosened again fails it; so does one tightened past an app's own query, which would black out project loading for all seven apps. Deliberately NOT in `npm test`: that suite must stay runnable with no emulator. Needs firebase-tools (a pinned devDependency here, so `npm ci` supplies the CLI) and a JRE (ubuntu-latest has one; scripts/run-rules-tests.mjs resolves Homebrew's keg-only JDK on macOS and EXITS NON-ZERO if it finds none \u2014 it never skips). No change to .github/workflows/shipgate.yml, which stays byte-identical across all nine repos."
    },
    {
      "name": "build (next build)",
      "run": "npm run build"
    },
    {
      "name": "functions \u2014 install",
      "run": "npm --prefix functions ci",
      "_note": "REQUIRED, and easy to miss. functions/ is a separate package with its own manifest and node_modules. The CI workflow runs `npm ci` at the ROOT only, so without this step the functions build fails in CI with `Cannot find type definition file for 'jest'` while passing locally, where functions/node_modules already exists. Putting it here rather than in the workflow keeps .github/workflows/shipgate.yml byte-identical across all nine repos \u2014 repo-specific detail belongs in this file."
    },
    {
      "name": "functions \u2014 lint",
      "run": "npm --prefix functions run lint",
      "_note": "functions/ predates the suite's move to Vitest and stays on Jest \u2014 two runners in one repository is deliberate, and they never interact."
    },
    {
      "name": "functions \u2014 build (tsc)",
      "run": "npm --prefix functions run build"
    },
    {
      "name": "functions \u2014 test (jest)",
      "run": "npm --prefix functions test",
      "_note": "Includes aiOpContractHash.test.ts, which pins the cross-repo digest of ai-op-contract.json. SPERT Scheduler pins the same constant in src/integration/ai-op-contract-hash.test.ts, so a one-sided edit to the contract now fails in the repo where it was made."
    }
  ]
}
